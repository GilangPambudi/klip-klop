# Release signing for the Klip-Klop APK. This keystore is committed so the
# APK keeps the SAME signature across CI builds — required to install over an
# existing app (otherwise Android treats each build as a different app).
#
# NOTE: personal sideload app; the keystore+password are intentionally kept
# simple. Do NOT use this keystore for anything public/sensitive.
storePassword=klipklop2026
keyPassword=klipklop2026
keyAlias=klipklop
storeFile=keystore/release.jks
